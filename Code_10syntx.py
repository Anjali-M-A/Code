# Syntax Error

sample = 10

# check that if sample is greater or not
if(sample>9)

    print("Sample is greater")

"""
   *** It returns a syntax error message because after if statement a colon : is missing.
   *** We can fix this by writing the correct syntax."""
    
