#AttributeError
"""
   **AttributeError can be defined as an error that is raised when an attribute reference or assignment fails.
   ** variable 'X' is a integer type it’s not support append method.
         So in this type of problem we get an error called “AttributeError“. 
"""

  
X = 10
  
# Raises an AttributeError 
X.append(6) 
